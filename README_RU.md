# JoinMessage

🌐 [English version](README.md)

Кастомные сообщения входа и выхода для Minecraft серверов с поддержкой конфига.

## Возможности

- Кастомные сообщения входа
- Кастомные сообщения выхода
- Поддержка цветовых кодов (&a, &c, &f и т.д.)
- Плейсхолдер {player} для имени игрока
- Простое редактирование конфига

## Установка

Закинь `JoinMessage.jar` в папку `plugins/` и перезапусти сервер.

## Конфиг

После первого запуска отредактируй `plugins/JoinMessage/config.yml`:

```yaml
messages:
  join: "&8[&a+&8] &a{player}"
  quit: "&8[&c-&8] &c{player}"
```

Используй `/reload confirm` или перезапусти сервер для применения изменений.

## Цветовые коды

- `&a` - зеленый
- `&c` - красный
- `&f` - белый
- `&7` - серый
- `&e` - желтый
- `&b` - голубой
- `&6` - золотой

## Совместимость

- Minecraft 1.16.5+
- Spigot / Paper / Purpur
- Java 16+

## Сборка

1. Скачай Spigot API 1.16.5 с [GetBukkit](https://getbukkit.org/download/spigot)
2. Положи его в `build/spigot-api.jar`
3. Запусти скрипт сборки:

```bash
# Windows
COMPILE.bat

# Linux/Mac
chmod +x compile.sh && ./compile.sh
```

## Лицензия

MIT

## Автор

Blurred

